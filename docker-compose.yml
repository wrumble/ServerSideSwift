version: "3.7"

networks:
  app-net:
    driver: bridge
    
services:
  app:
    build: .
    ports:
      - "8080:8080"
    links:
      - db:localhost
    networks:
      - app-net

  db:
    image: couchdb
    ports:
      - "5984:5984"
    environment:
      COUCHDB_USER: Test
      COUCHDB_PASSWORD: test
    networks:
      - app-net
